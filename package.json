{
  "name": "tracemap",
  "version": "1.0.0",
  "description": "A application to visualize the route a request travels with the help of traceroute/tracert",
  "scripts": {
    "dev": "concurrently --kill-others \"npm run be:dev\" \"npm run fe:dev\"",
    "be:dev": "npm --prefix backend run dev",
    "fe:dev": "npm --prefix frontend run dev"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.4.0",
    "@typescript-eslint/parser": "^7.4.0",
    "concurrently": "^8.2.2",
    "eslint": "^8.57.0"
  }
}
